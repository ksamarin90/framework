{
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:wev": "parcel index.html",
    "start": "concurrently npm:start:*"
  },
  "devDependencies": {
    "typescript": "^4.2.2"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "concurrently": "^6.0.0"
  }
}
